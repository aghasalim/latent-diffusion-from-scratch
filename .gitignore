.venv/
__pycache__/
*.pyc
*.so
.pytest_cache/
.ruff_cache/
.DS_Store
build/
dist/
*.egg-info/
*.ncu-rep

# results/ is deliberately NOT ignored -- benchmark CSVs are committed so the
# README tables stay reproducible.

# MNIST is downloaded on first use, not committed (9.6 MB)
data/

# derived from a retrain, not a measurement, so it is rebuilt rather than committed
results/denoise-frames.npz

# build products from verify/, never committed
verify/*/target/
verify/gocheck/gocheck
*.class
